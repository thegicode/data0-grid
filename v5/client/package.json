{
    "name": "client",
    "version": "1.0.0",
    "scripts": {
        "build": "node esbuild.config.js",
        "watch": "node esbuild.config.js --watch",
        "start": "npm run build"
    },
    "devDependencies": {
        "esbuild": "^0.14.0",
        "typescript": "^4.5.2"
    }
}
