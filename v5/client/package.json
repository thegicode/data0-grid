{
    "name": "client",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "start": "npm run build && live-server"
    },
    "devDependencies": {
        "typescript": "^4.5.2",
        "live-server": "^1.2.1"
    }
}
